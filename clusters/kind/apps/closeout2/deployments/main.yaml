apiVersion: apps/v1
kind: Deployment
metadata:
  name: "closeout2-main"
  labels: { app: "closeout2", deployment: "main" }
spec:
  replicas: 1
  selector:
    matchLabels: { app: "closeout2", deployment: "main" }
  template:
    metadata:
      labels: { app: "closeout2", deployment: "main" }
      annotations: { winda.deploy/env-checksum: "da427aa5d14ee23f" }
    spec:
      containers:
        - name: "closeout2"
          image: "docker.io/nginx:1.27-alpine"
          ports:
            - containerPort: 80
          env:
            - { name: "DEPLOYMENT", value: "main" }
          envFrom:
            - configMapRef: { name: "closeout2-main-env" }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: "closeout2-main"
spec:
  selector: { app: "closeout2", deployment: "main" }
  ports:
    - port: 80
      targetPort: 80
