apiVersion: apps/v1
kind: Deployment
metadata:
  name: "legacyapp-main"
  labels: { app: "legacyapp", deployment: "main" }
spec:
  replicas: 1
  selector:
    matchLabels: { app: "legacyapp", deployment: "main" }
  template:
    metadata:
      labels: { app: "legacyapp", deployment: "main" }
      annotations: { winda.deploy/env-checksum: "436d99a0b77afa49" }
    spec:
      containers:
        - name: "legacyapp"
          image: "docker.io/nginx:1.27-alpine"
          ports:
            - containerPort: 80
          env:
            - { name: "DEPLOYMENT", value: "main" }
          envFrom:
            - configMapRef: { name: "legacyapp-main-env" }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: "legacyapp-main"
spec:
  selector: { app: "legacyapp", deployment: "main" }
  ports:
    - port: 80
      targetPort: 80
