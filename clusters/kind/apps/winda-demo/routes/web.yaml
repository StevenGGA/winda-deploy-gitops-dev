apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "winda-demo-web"
  labels: { app: "winda-demo", staticSite: "web", workload: "static-site" }
spec:
  parentRefs:
    - { name: main-gateway, namespace: winda-deploy }
  hostnames: ["winda-demo-web.dev.localtest.me"]
  rules:
    - matches:
        - path: { type: PathPrefix, value: "/" }
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - { name: "X-Winda-Static-Bucket", value: "winda-static-dev" }
              - { name: "X-Winda-Static-Prefix", value: "winda-demo/web" }
              - { name: "X-Winda-Static-Mode", value: "spa" }
      backendRefs:
        - { name: "static-proxy", namespace: "winda-deploy", port: 8080 }
