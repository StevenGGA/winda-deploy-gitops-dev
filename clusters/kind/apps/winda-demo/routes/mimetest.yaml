apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "winda-demo-mimetest"
  labels: { app: "winda-demo", staticSite: "mimetest", workload: "static-site" }
spec:
  parentRefs:
    - { name: main-gateway, namespace: winda-deploy }
  hostnames: ["winda-demo-mimetest.dev.localtest.me"]
  rules:
    - matches:
        - path: { type: PathPrefix, value: "/" }
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - { name: "X-Winda-Static-Bucket", value: "winda-static-dev" }
              - { name: "X-Winda-Static-Prefix", value: "winda-demo/mimetest" }
              - { name: "X-Winda-Static-Mode", value: "docs" }
      backendRefs:
        - { name: "static-proxy", namespace: "winda-deploy", port: 8080 }
