apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-v1
  labels: { app: demo, deployment: v1 }
spec:
  replicas: 1
  selector:
    matchLabels: { app: demo, deployment: v1 }
  template:
    metadata:
      labels: { app: demo, deployment: v1 }
    spec:
      containers:
        - name: demo
          image: docker.io/nginx:latest
          ports:
            - containerPort: 80
          env:
            - { name: DEPLOYMENT, value: "v1" }
            - { name: PORT, value: "80" }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: demo-v1
spec:
  selector: { app: demo, deployment: v1 }
  ports:
    - port: 80
      targetPort: 80
