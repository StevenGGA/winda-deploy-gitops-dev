apiVersion: apps/v1
kind: Deployment
metadata:
  name: "demo-v1"
  labels: { app: "demo", deployment: "v1" }
spec:
  replicas: 1
  selector:
    matchLabels: { app: "demo", deployment: "v1" }
  template:
    metadata:
      labels: { app: "demo", deployment: "v1" }
      annotations: { winda.deploy/env-checksum: "324c3733146f66b8" }
    spec:
      containers:
        - name: "demo"
          image: "docker.io/nginx:latest"
          ports:
            - containerPort: 80
          env:
            - { name: "DEPLOYMENT", value: "v1" }
          envFrom:
            - configMapRef: { name: "demo-v1-env" }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: "demo-v1"
spec:
  selector: { app: "demo", deployment: "v1" }
  ports:
    - port: 80
      targetPort: 80
