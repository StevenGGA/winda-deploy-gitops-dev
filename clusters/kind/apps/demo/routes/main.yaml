apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: demo-main
spec:
  parentRefs:
    - { name: main-gateway, namespace: winda-deploy }
  hostnames: ["demo.dev.localtest.me"]
  rules:
    - matches:
        - path: { type: PathPrefix, value: / }
      backendRefs:
        - { name: demo-v1, port: 80, weight: 100 }
