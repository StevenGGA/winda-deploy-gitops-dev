apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "hello-site-web"
  labels: { app: "hello-site", staticSite: "web", workload: "static-site" }
spec:
  parentRefs:
    - { name: main-gateway, namespace: winda-deploy }
  hostnames: ["hello-site-web.dev.localtest.me"]
  rules:
    - matches:
        - path: { type: PathPrefix, value: "/" }
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - { name: "X-Winda-Static-Bucket", value: "winda-static-dev" }
              - { name: "X-Winda-Static-Prefix", value: "hello-site/web" }
              - { name: "X-Winda-Static-Mode", value: "docs" }
      backendRefs:
        - { name: "static-proxy", namespace: "winda-deploy", port: 8080 }
